export interface GradientOffsetData {
  now: number;
  increaseOffset: number;
  decreaseOffset: number;
}

export interface GradientOrder {
  startPrice: string;
  endPrice: string;
  startDate: string;
  endDate: string;
  budget: string;
  marginalPrice: string;
}
